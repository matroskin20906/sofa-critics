{% extends 'base.html.twig' %}




{% block title %}New user{% endblock %}

{% block avatar %} {% endblock %}
 
{% block pagebtn %}  {% endblock %}

{% block content %}
    
    <div id="userchargebox">
    <h1> Changing settings for {{ logedusername }} </h1>
    <div id="useravatarlogobox">
        {% set  mywaytofoto= logeduserphoto %}
        {% set  mywaytofoto= "userPhotos/" ~  mywaytofoto %}
        <img id="useravatarlogo" src="{{ asset( mywaytofoto ) }}" alt="" >
    </div>
    {{ form_start(userForm) }}

        {{ form_row(userForm.photoFile) }}
        {{ form_row(userForm.username) }}
        <button type="submit" class="btn">Change</button>
    {{ form_end(userForm) }}     


{#
    {{ form_start(userForm) }}
        {{ form_row(userForm.oldPassword, {
            label: 'Old Password'
        }) }}
        {{ form_row(userForm.newPassword, {
            label: 'New Password'
        }) }}
        <button type="submit" class="btn">Change</button>
    {{ form_end(userForm) }}
#}    
    </div>
{% endblock %}